import React from 'react';

function ErrorText({ text}) {
    let dtr = 'sds'

    return (
      

            <h1 className=' absolute text-red-600 text-sm mb-1  '>{ text}</h1>

        )
}

export default ErrorText;